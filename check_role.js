const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const user = await prisma.user.findUnique({ where: { email: 'amanshukla@cartigo.admin' } }); console.log(user ? user.role : 'NOT FOUND'); } main().finally(() => prisma.$disconnect());
