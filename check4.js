const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const apps = await prisma.resellerApplication.findMany({ include: { resellerProfile: { include: { user: { select: { id: true, email: true, name: true, createdAt: true } }, statusLog: { orderBy: { createdAt: 'desc' }, take: 10 }, documents: true } } }, orderBy: { createdAt: 'desc' } }); console.log('Apps:', apps.length); try { const mapped = apps.map(app => ({ profile: { id: app.resellerProfile.id } })); console.log('Mapped:', mapped.length); } catch (e) { console.error('Map error:', e); } } main().finally(() => prisma.$disconnect());
